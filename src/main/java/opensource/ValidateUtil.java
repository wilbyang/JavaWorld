package opensource;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class ValidateUtil {
	public static boolean isWantedLink(String link) {
		Pattern p = Pattern.compile("");     
		Matcher m = p.matcher("");  
		return m.matches();
	}
}
